
Modify the HTML file so that it looks exactly as shown in the following jpg image in
Google Chrome.

This_is_how_it_should_look.jpg



You are allowed to add styles in the HEAD part of the HTML document.

You are allowed to add styles within the body for those elements where 
style attribute is already available.

You are NOT allowed  add/modify/delete  any text/HTML_elements/HTML_attributes.

The image in the background is 1024 by 768 pixels.
The default font-size should be 15px.

IMPT: Use ony CSS 1, CSS 2.1 + 'opacity' CSS properties.
